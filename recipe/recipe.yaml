schema_version: 1

context:
  version: "0.0.14"

package:
  name: transfunctions
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/transfunctions/transfunctions-${{ version }}.tar.gz
  sha256: b04ceaba265fdf68b8a34d7c5322f8e8d2d94d1b664de4692e8d38140c7d179f

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools ==68.0.0
    - pip
  run:
    - python >=${{ python_min }}
    - displayhooks >=0.0.7
    - getsources >=0.0.4
    - typing-extensions
    

tests:
  - python:
      imports:
        - transfunctions
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: Say NO to Python fragmentation on sync and async
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/mutating/transfunctions

extra:
  recipe-maintainers:
    - thewchan
